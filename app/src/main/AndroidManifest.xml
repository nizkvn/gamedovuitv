<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nizk.tvq.dovuitv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity"
            android:theme="@style/SplashscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/MainscreenTheme"></activity>
        <activity android:name=".Nhapten"
            android:theme="@style/NhaptenscreenTheme"/>
        <activity android:name=".Play"
            android:theme="@style/PlayscreenTheme"/>
        <activity android:name=".Ketthuc"
            android:theme="@style/KetthucscreenTheme"/>
        <activity android:name=".kyniem"
            android:theme="@style/KetthucscreenTheme"/>
    </application>

</manifest>